# AI Listing Demo - 部署指南

## 方式一：Vercel 网页部署（最简单，推荐）

### 步骤 1：准备 GitHub 仓库
```bash
# 在项目根目录（不是 listing-ai-demo 目录）
git init
git add .
git commit -m "Initial commit: AI Listing Demo"

# 在 GitHub 创建新仓库后
git remote add origin https://github.com/你的用户名/ai-listing-demo.git
git push -u origin main
```

### 步骤 2：部署到 Vercel
1. 访问 https://vercel.com
2. 点击右上角 "Sign Up" 或 "Log In"
3. 选择 "Continue with GitHub"
4. 授权 Vercel 访问你的 GitHub
5. 点击 "Import Project"
6. 选择 "Import Git Repository"
7. 找到你的 `ai-listing-demo` 仓库
8. 点击 "Import"
9. **项目设置**：
   - Framework Preset: Next.js（自动检测）
   - Root Directory: `listing-ai-demo`（重要！）
   - Build Command: `npm run build`（自动填充）
   - Output Directory: `.next`（自动填充）
10. 点击 "Deploy"
11. 等待 2-3 分钟，部署完成！

### 步骤 3：获取访问链接
部署成功后，你会看到：
- 生产环境链接：`https://你的项目名.vercel.app`
- 可以绑定自定义域名

---

## 方式二：Vercel CLI 部署（命令行）

### 安装 Vercel CLI
```bash
npm install -g vercel
```

### 登录
```bash
vercel login
```

### 部署
```bash
cd listing-ai-demo
vercel
```

按提示操作：
1. Set up and deploy? **Y**
2. Which scope? 选择你的账号
3. Link to existing project? **N**
4. What's your project's name? **ai-listing-demo**
5. In which directory is your code located? **.**
6. Want to override the settings? **N**

等待部署完成，会给你一个链接！

### 部署到生产环境
```bash
vercel --prod
```

---

## 方式三：Netlify 部署

### 网页部署
1. 访问 https://netlify.com
2. 用 GitHub 登录
3. 点击 "Add new site" → "Import an existing project"
4. 选择 GitHub
5. 选择你的仓库
6. **构建设置**：
   - Base directory: `listing-ai-demo`
   - Build command: `npm run build`
   - Publish directory: `listing-ai-demo/.next`
7. 点击 "Deploy site"

---

## 方式四：Cloudflare Pages

1. 访问 https://pages.cloudflare.com
2. 登录并连接 GitHub
3. 选择仓库
4. **构建设置**：
   - Framework preset: Next.js
   - Build command: `npm run build`
   - Build output directory: `.next`
   - Root directory: `listing-ai-demo`
5. 点击 "Save and Deploy"

---

## 环境变量（如果需要）

如果将来需要添加 API 密钥等环境变量：

### Vercel
1. 进入项目设置
2. 点击 "Environment Variables"
3. 添加变量，例如：
   - `NEXT_PUBLIC_API_URL`
   - `API_SECRET_KEY`

### 在代码中使用
```typescript
const apiUrl = process.env.NEXT_PUBLIC_API_URL;
```

---

## 自定义域名

### Vercel
1. 项目设置 → Domains
2. 添加你的域名
3. 按提示配置 DNS（添加 CNAME 记录）

### DNS 配置示例
```
类型: CNAME
名称: www
值: cname.vercel-dns.com
```

---

## 常见问题

### Q: 部署失败怎么办？
A: 检查以下几点：
1. Root Directory 是否设置为 `listing-ai-demo`
2. Node.js 版本是否兼容（建议 18.x 或 20.x）
3. 查看构建日志，找到具体错误

### Q: 如何更新部署？
A: 推送代码到 GitHub，Vercel 会自动重新部署

### Q: 如何回滚到之前的版本？
A: Vercel 控制台 → Deployments → 选择之前的版本 → Promote to Production

### Q: 部署后页面空白？
A: 检查浏览器控制台是否有错误，通常是路径问题

### Q: 中国访问速度慢？
A: 
1. Vercel 在香港有节点，已在配置中指定
2. 或考虑使用国内平台（需要备案）

---

## 部署检查清单

部署前确认：
- [ ] 代码已推送到 GitHub
- [ ] `npm run build` 本地构建成功
- [ ] 没有硬编码的本地路径
- [ ] 环境变量已配置（如果有）
- [ ] Root Directory 设置正确

部署后测试：
- [ ] 登录功能正常
- [ ] 首页显示正常
- [ ] 竞品分析功能正常
- [ ] Listing 生成功能正常
- [ ] 资源页面正常
- [ ] 设置页面正常

---

## 方式五：腾讯云 EdgeOne 部署（国内访问快）

### 前提条件
- 已有腾讯云账号
- 已开通 EdgeOne 服务
- 已创建站点

### 步骤 1：生成部署包
项目已经生成好了部署包：`listing-ai-demo/edgeone-deploy.zip`（439KB）

如果需要重新生成：
```bash
cd listing-ai-demo
npm run build
cd out
zip -r ../edgeone-deploy.zip .
cd ..
```

### 步骤 2：上传到 EdgeOne

#### 方法 A：通过控制台上传（推荐）
1. 登录腾讯云 EdgeOne 控制台
2. 进入你的站点管理
3. 找到"静态资源托管"或"文件管理"
4. **重要**：清空现有文件（如果有）
5. 上传 `edgeone-deploy.zip`
6. **关键步骤**：解压到站点根目录
   - 不要创建 `out/` 文件夹
   - 确保 `index.html` 在根目录
   - 确保 `_next/` 文件夹在根目录

#### 方法 B：使用 FTP/SFTP
如果 EdgeOne 提供 FTP 访问：
1. 使用 FTP 客户端连接
2. 进入站点根目录
3. 上传 `listing-ai-demo/out/` 目录下的所有文件
4. 确保目录结构：
```
/
├── index.html          ← 必须在根目录
├── _next/
│   └── static/
├── favicon.ico
├── file.svg
├── globe.svg
└── ...
```

### 步骤 3：验证部署
1. 访问你的 EdgeOne 域名
2. 应该看到登录页面
3. 检查浏览器控制台，确保没有 404 错误

### 常见问题排查

#### 问题：访问显示 404 NOT_FOUND
**原因**：`index.html` 不在根目录

**解决方案**：
1. 检查文件结构，确保 `index.html` 在根目录
2. 如果上传了 `out/` 文件夹，需要把里面的文件移到根目录
3. 正确的结构：
   ```
   ✅ 正确：/index.html
   ❌ 错误：/out/index.html
   ```

#### 问题：页面空白或样式丢失
**原因**：`_next/` 目录路径不对

**解决方案**：
1. 确保 `_next/` 文件夹在根目录
2. 检查浏览器控制台的 404 错误
3. 确认 EdgeOne 的静态资源路径配置

#### 问题：刷新页面 404
**原因**：单页应用路由问题

**解决方案**：
在 EdgeOne 配置重写规则：
- 匹配：`/*`
- 重写到：`/index.html`

---

## 推荐配置

### package.json 确认
确保有以下脚本：
```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  }
}
```

### .gitignore 确认
确保忽略以下文件：
```
node_modules/
.next/
out/
.env*.local
.DS_Store
*.zip
```

---

## 获取帮助

- Vercel 文档: https://vercel.com/docs
- Next.js 部署: https://nextjs.org/docs/deployment
- Vercel 社区: https://github.com/vercel/vercel/discussions
- 腾讯云 EdgeOne: https://cloud.tencent.com/document/product/1552

---

## 部署成功后

你会获得：
- 一个公开访问的 URL
- 自动 HTTPS
- 全球 CDN 加速
- 自动部署（推送代码即更新）
- 每个 PR 的预览环境

现在就可以分享给其他人体验了！🎉
